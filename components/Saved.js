import { View,Text } from "react-native";

export default function SavedJobScreen() {
    return (
        <View style={{
            flexDirection:'row',justifyContent:'center',alignContent:'center'
        }}>
<Text> Saved Job Screen</Text>
        </View>
    )
}